import http from "./http"
import setting from "./setting"
import { get_storage, get_item, set_item, remove_item, clear_storage } from "./storage"
import {
    set_title,
    change_favicon,
    loading,
    message,
    verify,
    tree_data,
    remove_field,
    obj_update
} from "./other"

export {
    http,
    setting,
    get_item,
    get_storage,
    set_item,
    remove_item,
    clear_storage,
    set_title,
    change_favicon,
    loading,
    message,
    verify,
    tree_data,
    remove_field,
    obj_update
}